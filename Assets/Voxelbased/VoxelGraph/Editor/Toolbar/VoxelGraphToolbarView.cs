using GraphProcessor;
using UnityEngine;
using VoxelGraph.Processor;

namespace VoxelGraph.Editor.Toolbar
{
    public class VoxelGraphToolbarView : ToolbarView
    {
        public VoxelGraphToolbarView(BaseGraphView graphView) : base(graphView) {}

        protected override void AddButtons()
        {
            base.AddButtons();
        }
    }
}